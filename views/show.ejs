<% include partials/header %>

<div class="ui main text container segment">
    <div class="ui huge header"> <%= writeup.title %> </div> 
    <div class="ui top attached">
        <div class="item">
            <img class="ui centered rounded image" src = "<%=writeup.image%>">
            <div class="content">
                 <span><strong><%= writeup.created.toDateString() %></strong></span>
            </div>
             <div class="description">
                  <p><%- writeup.body %></p> 
             </div>
             
             <a class="btn btn-success" href="/writeups/<%= writeup._id %>/edit">EDIT</a>
             
             <form id="delete" action="/writeups/<%= writeup._id %>?_method=DELETE" method="POST">
                 <button class="btn btn-success">DELETE</button>
             </form>
             
        </div>
    </div>
    <br>
    
    <div class="well">
             
                    <div class="text-right">
                        <a class="btn btn-success" href="/writeups/<%= writeup._id %>/comments/new">Add New Comment</a>
                    </div>
                    
                    <hr>
                    
                    <% writeup.comments.forEach(function(comment){ %>
                         <div class="row">
                             <div class="col-md-12">
                                 <strong><%= comment.author %></strong>
                                 <span class="pull-right">10 days ago</span>
                                  <p>
                                     <%= comment.text %>
                                  </p>
                             </div>
                         </div>
                    <% }) %>
               </div>
</div>

        
<% include partials/footer %> 